{
  "name": "@blazyts/blazy-edge",
  "type": "module",
  "private": true,
  "module": "index.ts",
  "scripts": {
    "lint": "bun --bun eslint src tests",
    "build": "bunx tsc -p tsconfig.json --noEmit",
    "test": "bun x vitest run --config vitest.config.ts"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "devDependencies": {
    "@blazyts/backend-lib": "workspace:*",
    "@blazyts/better-standard-library": "workspace:*",
    "@types/bun": "latest"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1000.0",
    "zod": "^4.3.6"
  }
}
